export enum Elevation {
    LOWEST,
    LOW,
    NORMAL,
    HIGH,
    HIGHEST
}

export enum ImageDisplayMode {
    SONG,
    VOCALIST,
    PRODUCER
}

export interface RankingsGridColumns {
    extraLarge: number
    large: number
    medium: number
    small: number
    default: number
}